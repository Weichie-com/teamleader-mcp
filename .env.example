# ============================================================================
# Teamleader MCP Server - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Teamleader OAuth Access Token
# ----------------------------------------------------------------------------
# Your Teamleader Focus API access token.
# Obtain this through the OAuth 2.0 flow - see README.md for instructions.
TEAMLEADER_ACCESS_TOKEN=

# ----------------------------------------------------------------------------
# OPTIONAL: OAuth Credentials for Token Refresh
# ----------------------------------------------------------------------------
# Required if you want automatic token refresh functionality.
# Get these from https://marketplace.focus.teamleader.eu/build

# Your integration's client ID
TEAMLEADER_CLIENT_ID=

# Your integration's client secret (keep this secret!)
TEAMLEADER_CLIENT_SECRET=

# The redirect URI configured for your integration
TEAMLEADER_REDIRECT_URI=http://localhost:3000/callback

# Refresh token for obtaining new access tokens
TEAMLEADER_REFRESH_TOKEN=

# Custom path for token storage (default: ~/.teamleader-tokens.json)
# TEAMLEADER_TOKEN_STORAGE=/path/to/.teamleader-tokens.json

# ----------------------------------------------------------------------------
# OPTIONAL: SMTP Configuration for Email
# ----------------------------------------------------------------------------
# Only needed if you want to send emails directly through SMTP
# (The server can also use Teamleader's built-in email sending)

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false

# ----------------------------------------------------------------------------
# OPTIONAL: Development Settings
# ----------------------------------------------------------------------------

# Enable debug logging (true/false)
DEBUG=false

# API base URL (only change for testing)
# TEAMLEADER_API_URL=https://api.focus.teamleader.eu
